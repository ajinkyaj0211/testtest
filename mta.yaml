ID: testtest
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: testtest_db
   type: hdb
   path: testtest_db
   requires:
    - name: hdi_testtest_db

resources:
 - name: hdi_testtest_db
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
